console.log("Servidor da Barbearia iniciado com sucesso!");

// Exemplo simples de lógica
const saudacao = (nome: string) => {
  return `Bem-vindo à Barbearia, ${nome}!`;
};

console.log(saudacao("Cliente"));
